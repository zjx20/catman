帮我做一个跑在openwrt docker 里的AI助手，它是基于 claude 的（agent sdk 或者 claude cli -p），可以使用 claude 订阅，能够在docker内执行任意指令；我期望它能够像claude code一样拥有足够的能力去操作环境。然后有一个轻量聊天gateway，能够对接微信、钉钉等聊天工具，作为一个bot来接收用户指令；再配备一个http dashboard，用来显示会话记录。

由于微信里面只有一个聊天窗口，没有“新建会话”这样可以隔离会话的交互，因此会话管理这块要智能一些，大致遵循以下规则：
* 上下文太长时自动压缩，后续工作在压缩后的上下文（即跟用户看到的聊天记录其实不一样），完整聊天记录可以放到文件里；这一块看看 sdk 是否有现成能力
* 1小时内用户的新输入归作当前会话的一部分，否则直接开新会话；后台到点自动推送一条会话超时消息，但这个时候不会马上开新会话，允许用户继续会话，推送内容示例 “下次发消息将在新会话中处理，如需继续会话，请输入"继续"”（文案你可以写好一点）
* 拥有某种轻量的手段可以检索旧会话，把会话持久化到json文件里，检索时使用grep
* 30天之前的旧会话直接删掉，避免无限膨胀
